/* ----------------------------------------------------------------------------------------------
courseApi.js
This file does all the course API calls 
------------------------------------------------------------------------------------------------- */

import apiSlice from "../base/apiSlice";

const courseApi = apiSlice.injectEndpoints({
  endpoints: (builder) => ({}),
});

export const {} = courseApi;
